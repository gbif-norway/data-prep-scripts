apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml
    kompose.version: 1.31.2 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: script-service
  name: script-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: script-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml
        kompose.version: 1.31.2 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/data-prep-scripts-default: "true"
        io.kompose.service: script-service
    spec:
      containers:
        - command:
            - /bin/bash
          image: dataprep:latest
          name: script-service
          ports:
            - containerPort: 8888
              hostPort: 8888
              protocol: TCP
          resources: {}
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /srv/scripts
              name: script-service-claim0
      restartPolicy: Always
      volumes:
        - name: script-service-claim0
          persistentVolumeClaim:
            claimName: script-service-claim0
status: {}
